.panel.panel-primary
    .panel-heading.text-center new
    .panel-body
        .row(ng-repeat="course in courses | orderBy: 'published': true | limitTo:5")
            .col-md-3 {{course.published | date: "MMM d"}}
            .col-md-9 {{course.name}}
